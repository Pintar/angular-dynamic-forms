import { FormItemHostDirective } from './form-item-host.directive';

describe('FormItemHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FormItemHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
